{
  "name": "gym-locker-system",
  "version": "1.0.0",
  "description": "Gym Locker Management System",
  "main": "index.js",
  "scripts": {
    "start": "node scripts/start.js",
    "start:force": "node scripts/start.js --force",
    "start:backend": "node gym_lockers/server.js",
    "start:frontend": "cd gym_lockers/client && npm start",
    "install:all": "npm install && cd gym_lockers && npm install && cd client && npm install",
    "build": "cd gym_lockers/client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.2",
    "dotenv": "^16.3.1",
    "find-process": "^1.4.7",
    "tcp-port-used": "^1.0.2"
  }
}